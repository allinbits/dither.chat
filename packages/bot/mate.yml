# Telegram Mini Apps Mate Configuration
# https://docs.telegram-mini-apps.com/packages/telegram-apps-mate

# Development configuration
dev:
  port: 5173
  host: localhost

# Deployment configuration (to be filled after project creation)
deploy:
  # projectId: # Will be set after creating project with @tma_mate_bot
  # token: # Will be set after creating project with @tma_mate_bot
  directory: dist
  tag: latest
